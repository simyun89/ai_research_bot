name: dummy-test-weekdays

on:
  workflow_dispatch:

jobs:
  dummy-run:
    runs-on: ubuntu-latest
    steps:
      - name: 허수 Step (테스트용)
        run: |
          echo "✅ 허수 테스트 실행 중입니다."
          echo "실행 시각: $(date)"
